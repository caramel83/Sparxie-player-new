# ملاحظات النشر على Railway + ملخص التعديلات

## ⚠️ مهم جداً قبل الرفع — تخزين البيانات

البوت يحفظ النقاط (`scores.json`) وحالة السؤال اليومي (`daily_state.json`) في ملفات محلية
على القرص. **Railway يمسح القرص في كل Deploy جديد إلا إذا أضفت Volume** (من تبويب
Settings → Volumes بمشروعك بـ Railway، وتربطه بمسار المشروع نفسه).

لو ما ضفت Volume:
- كل مرة تعمل Deploy (تحديث كود، ريستارت، إلخ) → النقاط والسؤال اليومي النشط يرجعون للصفر.
- هذا ما يأثر على باقي وظائف البوت، بس النقاط لازم تتابعها.

**الحل:** أضف Volume بـ Railway واربطه بمجلد المشروع (root) عشان `scores.json` و
`daily_state.json` يفضلون محفوظين بين كل Deploy.

---

## متغيرات البيئة الجديدة (.env)

| المتغير | الوصف | افتراضي |
|---|---|---|
| `GAME_CHANNEL_IDS` | قنوات الألعاب اللي يشتغل فيها البريفكس العربي (`!رتب`, `!خمن`...). آيديات مفصولة بفاصلة. فاضي = يشتغل بكل مكان | فاضي |
| `DAILY_CHANNEL_ID` | القناة اللي ينشر فيها السؤال اليومي تلقائياً. لازم تحددها وإلا يصير معطّل | فاضي |
| `DAILY_HOUR_UTC` | الساعة (UTC، 0-23) اللي يتفحّص فيها انتهاء/بداية السؤال اليومي | 0 |
| `AUTO_GAME_CHANNEL_ID` | (موجود مسبقاً) قناة الألعاب التلقائية | فاضي |
| `MEME_CHANNEL_ID` | (موجود مسبقاً) قناة الميمز التلقائية | فاضي |

راجع `.env.example` للتفاصيل الكاملة.

---

## ملخص التعديلات الكبرى

1. **نظام ألعاب موحّد (`gameEngine.js`)** يدعم وضعين لكل لعبة:
   - **جماعي مفتوح**: `/أمر` أو `!بريفكس` بدون منشن — أي شخص بالقناة يجاوب
   - **تحدي شخص محدد**: نفس الأمر + منشن شخص (`/أمر الخصم:@شخص` أو `!بريفكس @شخص [جولات]`)
     — يبدأ التحدي مع هذا الشخص، **وبقية القناة تقدر تشارك بالإجابة** (النقاط العادية)،
     لكن **فوز التحدي (+20)** يُحسب فقط للاعبين الأساسيين.

2. **الألعاب المدمجة بالمحرك الموحّد:**
   `/scramble` (رتب الكلمة), `/guess` (خمن الشخصية بـ 4 أزرار), `/trivia` (سؤال ثقافي),
   `/battle` (أسئلة HSR), `/truth` (صراحة), `/wouldyourather` (تفضيل).

3. **`/خمن` (guess) الجديد:** بدل الكتابة بالشات، 4 أزرار اختيار (إجابة صح + 3 مشتتات
   عشوائية بالكامل). الترتيب: السؤال → الأزرار → تخطي/إيقاف تحتها. نفس النقاط (+15).

4. **`/daily` أصبح تلقائياً بالكامل:**
   - ينشر بقناة `DAILY_CHANNEL_ID` بالساعة المحددة بـ `DAILY_HOUR_UTC`
   - سؤال اختيار من متعدد (عنصر/مسار/ندرة لشخصية HSR)
   - يفضل مفتوح 24 ساعة، التصويت صامت (بدون إعلان فوري)
   - بعد 24 ساعة: إعلان الإجابة + توزيع نقاط بترتيب أول صحيح (25/20/15/10، والباقي 5)
     + إطلاق سؤال جديد فوراً
   - أمر `/daily` نفسه الآن بس معلوماتي (يوجّه للقناة المخصصة)

5. **البريفكس العربي (`!`)** يعمل على كل الأوامر (الألعاب + rate/ship/fact/joke/slap/
   gunxie/meme/cuddle/pat/bonk/8ball/weather/remind/quote/xo/duel)، لكن **بس بالقنوات
   المحددة بـ `GAME_CHANNEL_IDS`**. راجع `/help` لقائمة الاختصارات الكاملة.

6. **`/help` محدّث بالكامل** بكل الأوامر الفعلية (تمت إزالة أي أمر قديم غير موجود).

7. **نكت إضافية** أُضيفت لقاعدتي `normal_jokes.js` و `hsr_jokes.js` (بنكهة Sparxie
   كستريمر).

8. ملفات قديمة (`gameLauncher.js`) أصبحت "compat shim" بس — تحول كل الاستدعاءات
   لـ `gameEngine.js` الجديد بدون كسر أي ملف قديم يستورد منها.

---

## بنية الملفات الجديدة

```
config.js          — قراءة متغيرات البيئة المركزية
gameEngine.js       — محرك الألعاب الموحّد (جماعي + تحدي)
gameManager.js       — حالة الألعاب/التحديات النشطة بكل قناة (في الذاكرة)
dailyManager.js      — نظام السؤال اليومي التلقائي بالكامل
prefixRouter.js      — موجّه البريفكس العربي (!)
data/questionBank.js — بنك الأسئلة الموحّد (ثقافي/صراحة/تفضيل/HSR)
gameLauncher.js       — (قديم) compat shim يحوّل لـ gameEngine.js
```
